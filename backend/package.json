{
  "name": "vulnerable-insurance-backend",
  "version": "1.0.0",
  "description": "Vulnerable insurance management system for security training",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "init-db": "node dist/init-db.js",
    "clear-db": "node dist/clear-db.js"
  },
  "dependencies": {
    "fastify": "^4.24.3",
    "@fastify/cors": "^8.4.0",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "@types/node": "^20.8.7",
    "typescript": "^5.2.2",
    "ts-node-dev": "^2.0.0"
  }
}
